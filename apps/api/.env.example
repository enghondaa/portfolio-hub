# Server
PORT=4000
NODE_ENV=development

# CORS: comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:3001

# MongoDB Atlas connection string (added in Phase 3)
# MONGODB_URI=

# JWT secrets (added in Phase 3)
# JWT_ACCESS_SECRET=
# JWT_REFRESH_SECRET=
